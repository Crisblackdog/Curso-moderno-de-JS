const nombre = "monitor 20 pulgadas";
const precio = 300;
const disponible = true;

//obeto agruupa todo en un variable

const producto = {
    nombre: "monitor 20 pulgadas",
    precio: 300,
    disponible: true
}